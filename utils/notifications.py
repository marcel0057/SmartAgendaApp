# utils/notifications.py
def send_notification(user_id, message):
    # Placeholder pour une logique d'envoi de notification (par exemple, email ou push)
    print(f"Notification envoyée à {user_id}: {message}")